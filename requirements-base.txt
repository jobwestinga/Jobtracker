# Shared by the desktop app and the server: the pure-Python core has no
# third-party runtime dependencies at all, so this file is intentionally empty
# of packages and exists as the seam for future shared deps.
